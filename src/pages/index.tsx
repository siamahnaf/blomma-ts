import type { NextPage } from "next";

//Layout
import Layout from "Layout";

const Home: NextPage = () => {
  return (
    <Layout active="home">
      Hello world
    </Layout>
  );
};
export default Home;